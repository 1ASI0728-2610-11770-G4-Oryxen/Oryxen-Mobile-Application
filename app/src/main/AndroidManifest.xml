<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Oryxen"
        android:usesCleartextTraffic="${usesCleartextTraffic}">

        <!-- Resolved per build type in app/build.gradle.kts: false for release (HTTPS-only
             against Render) and for debug builds that use the deployed backend; true only
             when -PapiBaseUrl points debug at a plaintext local backend. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Oryxen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
